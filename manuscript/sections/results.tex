The results report leakage-free forecasting, calibration, transfer, service-level evidence, and control outcomes. They support a boundary-mapping claim rather than a universal model-improvement claim.

\subsection{Forecasting benchmark}

Table~\ref{tab:forecast-leakage-free} summarizes aggregate forecasting on 11{,}208 held-out samples. Linear regression has the best average MAE (3.514), random forest is close (3.529), and \texttt{UA-MSTCN-Lite} remains competitive rather than dominant (3.548). The MLP improves over simple heuristics but does not change the ranking. At the 10-minute horizon, linear regression and \texttt{UA-MSTCN-Lite} differ by only 0.0013 MAE, so the aggregate result supports using the surrogate for control while rejecting a point-forecast superiority claim.

\begin{table}[htbp]
  \centering
  \caption{Leakage-free forecasting performance on the aggregate cluster series (MAE, RMSE, MAPE).}
  \label{tab:forecast-leakage-free}
  \scriptsize
  \setlength{\tabcolsep}{2pt}
  \renewcommand{\arraystretch}{0.96}
  \begin{tabular}{lcccc}
    \toprule
    Model & Horizon & MAE & RMSE & MAPE \\
    \midrule
    Persistence & 1 & 2.395 & 3.285 & 0.060 \\
    Moving average & 1 & 3.215 & 4.348 & 0.079 \\
    Linear regression & 1 & 2.268 & 3.123 & 0.056 \\
    Random forest & 1 & 2.324 & 3.176 & 0.058 \\
    MLP regressor & 1 & 2.422 & 3.267 & 0.061 \\
    UA-MSTCN-Lite & 1 & 2.349 & 3.214 & 0.058 \\
    Persistence & 5 & 4.569 & 6.162 & 0.112 \\
    Moving average & 5 & 4.387 & 5.850 & 0.107 \\
    Linear regression & 5 & 3.967 & 5.324 & 0.098 \\
    Random forest & 5 & 3.928 & 5.237 & 0.097 \\
    MLP regressor & 5 & 4.276 & 5.603 & 0.105 \\
    UA-MSTCN-Lite & 5 & 3.986 & 5.304 & 0.098 \\
    Persistence & 10 & 5.119 & 6.785 & 0.126 \\
    Moving average & 10 & 4.845 & 6.409 & 0.119 \\
    Linear regression & 10 & 4.307 & 5.765 & 0.107 \\
    Random forest & 10 & 4.334 & 5.780 & 0.107 \\
    MLP regressor & 10 & 4.427 & 5.781 & 0.109 \\
    UA-MSTCN-Lite & 10 & 4.308 & 5.793 & 0.107 \\
    \bottomrule
  \end{tabular}
\end{table}

Table~\ref{tab:aggregate-qlr-baseline} adds the reviewer-requested QLR baseline, fitted only on the train-plus-validation prefix and evaluated only on the test segment. QLR is competitive at the 1- and 5-minute horizons, but it is used as an auditable uncertainty baseline rather than a new superiority claim. Raw and guarded $P90$ outputs are retained, with zero raw and guarded crossings in the aggregate run.

\begin{table}[htbp]
  \centering
  \caption{Aggregate quantile linear regression baseline on the held-out test split.}
  \label{tab:aggregate-qlr-baseline}
  \scriptsize
  \setlength{\tabcolsep}{2pt}
  \renewcommand{\arraystretch}{0.96}
  \begin{tabular}{lccccc}
    \toprule
    Model & H & MAE & RMSE & $P90$ cov. & Raw crosses \\
    \midrule
    QLR & 1 & 2.265 & 3.145 & 0.900 & 0 \\
    QLR & 5 & 3.967 & 5.390 & 0.883 & 0 \\
    QLR & 10 & 4.315 & 5.861 & 0.886 & 0 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../experiments/results/figures/baseline_mae_by_horizon.png}
  \caption{Forecast error by model and horizon after the leakage-free split correction. Linear regression remains the strongest average point forecaster, while the standard neural MLP baseline improves over simple heuristics without changing the overall ranking.}
  \label{fig:mae-by-horizon}
\end{figure}

\subsection{Case study and accuracy-runtime trade-off}

Figure~\ref{fig:forecast-case-study} plots aligned test slices for the 1-, 5-, and 10-minute horizons. Persistence lags at turns, the MLP smooths turning points, and random forest and \texttt{UA-MSTCN-Lite} track the main shape more closely. The figure remains relevant because the controller consumes the surrogate's quantile band, not only its median.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../experiments/results/figures/forecast_case_study.png}
  \caption{Forecast case study on aligned test slices for the 1-, 5-, and 10-minute horizons, with persistence, linear regression, random forest, MLP regressor, and \texttt{UA-MSTCN-Lite} predictions overlaid.}
  \label{fig:forecast-case-study}
\end{figure}

Figure~\ref{fig:latency-tradeoff} compares average MAE with runtime. Linear regression is strongest and fastest among the main point baselines at 19.3 ms per call; the MLP takes about 630 ms; random forest is slightly more accurate than MLP but much slower. \texttt{UA-MSTCN-Lite} stays close to random forest in MAE while adding quantile outputs, separating point accuracy from controller-facing uncertainty.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../experiments/results/figures/forecast_latency_tradeoff.png}
  \caption{Average forecasting accuracy versus average runtime per benchmark call on the current workstation.}
  \label{fig:latency-tradeoff}
\end{figure}

\subsection{Context, regime, and calibration analysis}

Figure~\ref{fig:context-sensitivity} shows context-window sensitivity. A 60-minute window gives the best average MAE (3.548), compared with 3.615 for 30 minutes and 3.585 for 120 minutes.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.76\linewidth]{../experiments/results/figures/ua_context_sensitivity.png}
  \caption{Context-window sensitivity of \texttt{UA-MSTCN-Lite}. The 60-minute default gives the best average MAE across the three evaluation horizons.}
  \label{fig:context-sensitivity}
\end{figure}

Figure~\ref{fig:regime-comparison} partitions the 5-minute test set by recent volatility. \texttt{UA-MSTCN-Lite} is slightly better than random forest in the moderate regime (3.731 vs.\ 3.752 MAE), but slightly worse in stable and bursty regimes, identifying bursty demand as the clearest target for a stronger model.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.76\linewidth]{../experiments/results/figures/forecast_regime_comparison.png}
  \caption{Forecast error at the 5-minute horizon across stable, moderate, and bursty workload regimes.}
  \label{fig:regime-comparison}
\end{figure}

Table~\ref{tab:quantile-coverage} and Figure~\ref{fig:quantile-coverage} evaluate quantile coverage. $P50$ stays close to 0.5, while $P90$ is conservative at 1 minute (0.920) and mildly under-covered at 5 and 10 minutes (0.867 and 0.868). The band is usable for control, but longer-horizon under-coverage justifies the explicit scale-out margin.

\begin{table}[htbp]
  \centering
  \caption{Empirical quantile coverage of \texttt{UA-MSTCN-Lite} on the aggregate test split.}
  \label{tab:quantile-coverage}
  \scriptsize
  \setlength{\tabcolsep}{3pt}
  \renewcommand{\arraystretch}{0.96}
  \begin{tabular}{lccc}
    \toprule
    Horizon & $P50$ cov. & $P90$ cov. & Mean int. width \\
    \midrule
    1 minute & 0.506 & 0.920 & 4.623 \\
    5 minutes & 0.516 & 0.867 & 6.709 \\
    10 minutes & 0.500 & 0.868 & 7.405 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.72\linewidth]{../experiments/results/figures/quantile_coverage.png}
  \caption{Empirical $P50$ and $P90$ coverage of \texttt{UA-MSTCN-Lite}. Dashed lines indicate the nominal target levels of 0.5 ($P50$) and 0.9 ($P90$).}
  \label{fig:quantile-coverage}
\end{figure}

\subsection{Cross-entity machine-cohort benchmark}

The 12-machine cohort tests whether the aggregate story survives a finer unit of analysis. Table~\ref{tab:machine-cohort-summary} shows the same direction, but more strongly: linear regression is the best point predictor at every horizon (MAE 3.633, 5.520, and 5.966), the MLP is second, and \texttt{UA-MSTCN-Lite} trails both MLP and random forest on this cohort.

\begin{table}[htbp]
  \centering
  \caption{Average forecasting performance across the 12-machine high-coverage cohort.}
  \label{tab:machine-cohort-summary}
  \tiny
  \setlength{\tabcolsep}{2pt}
  \renewcommand{\arraystretch}{0.95}
  \begin{tabular}{lcccc}
    \toprule
    Model & H & MAE & RMSE & MAPE \\
    \midrule
    Persistence & 1 & 3.716 & 5.185 & 0.103 \\
    Linear regression & 1 & 3.633 & 4.926 & 0.101 \\
    MLP regressor & 1 & 3.921 & 5.246 & 0.109 \\
    Random forest & 1 & 4.012 & 5.396 & 0.107 \\
    UA-MSTCN-Lite & 1 & 4.121 & 5.647 & 0.106 \\
    Persistence & 5 & 6.268 & 8.497 & 0.172 \\
    Linear regression & 5 & 5.520 & 7.318 & 0.153 \\
    MLP regressor & 5 & 5.845 & 7.683 & 0.159 \\
    Random forest & 5 & 6.170 & 8.223 & 0.162 \\
    UA-MSTCN-Lite & 5 & 6.244 & 8.423 & 0.161 \\
    Persistence & 10 & 6.932 & 9.390 & 0.192 \\
    Linear regression & 10 & 5.966 & 7.959 & 0.165 \\
    MLP regressor & 10 & 6.301 & 8.324 & 0.172 \\
    Random forest & 10 & 6.548 & 8.745 & 0.173 \\
    UA-MSTCN-Lite & 10 & 6.651 & 8.936 & 0.172 \\
    \bottomrule
  \end{tabular}
\end{table}

Figure~\ref{fig:machine-cohort-mae} shows the same pattern by entity. Linear regression wins 6, 8, and 8 of the 12 machines at the 1-, 5-, and 10-minute horizons; \texttt{UA-MSTCN-Lite} wins 2, 3, and 2. The current surrogate is therefore not the strongest general-purpose point forecaster at finer granularity.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.78\linewidth]{../experiments/results/machine_cohort/figures/machine_cohort_mae_by_horizon.png}
  \caption{Average forecasting MAE across the 12-machine high-coverage cohort.}
  \label{fig:machine-cohort-mae}
\end{figure}

Figure~\ref{fig:machine-cohort-entity} shows that most of the gap comes from a subset of machines. The worst case is \texttt{m\_2014}, where \texttt{UA-MSTCN-Lite} reaches 9.37 average MAE versus 6.04 for linear regression.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.78\linewidth]{../experiments/results/machine_cohort/figures/machine_cohort_entity_mae.png}
  \caption{Per-machine average MAE across the 12-machine cohort. Each point is one machine and the horizontal segment marks the model mean.}
  \label{fig:machine-cohort-entity}
\end{figure}

The uncertainty result is more stable than the point-ranking result. Table~\ref{tab:machine-cohort-coverage} shows average $P50$ coverage near 0.5 and $P90$ coverage between 0.847 and 0.891, so longer-horizon under-coverage is not unique to the aggregate series.

\begin{table}[htbp]
  \centering
  \caption{Average quantile coverage of \texttt{UA-MSTCN-Lite} across the 12-machine cohort.}
  \label{tab:machine-cohort-coverage}
  \scriptsize
  \setlength{\tabcolsep}{3pt}
  \renewcommand{\arraystretch}{0.96}
  \begin{tabular}{lccc}
    \toprule
    Horizon & $P50$ cov. & $P90$ cov. & Mean int. width \\
    \midrule
    1 minute & 0.510 & 0.891 & 7.529 \\
    5 minutes & 0.523 & 0.853 & 9.375 \\
    10 minutes & 0.517 & 0.847 & 9.825 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.72\linewidth]{../experiments/results/machine_cohort/figures/machine_cohort_quantile_coverage.png}
  \caption{Average $P50$ and $P90$ coverage of \texttt{UA-MSTCN-Lite} across the 12-machine cohort.}
  \label{fig:machine-cohort-coverage}
\end{figure}

\subsection{Cross-machine transfer}

The transfer benchmark trains pooled global models on 8 machines and evaluates zero-shot on 4 held-out machines, repeated over 3 deterministic folds. Table~\ref{tab:machine-transfer} summarizes the result.

\begin{table}[htbp]
  \centering
  \caption{Cross-machine zero-shot transfer on held-out machines.}
  \label{tab:machine-transfer}
  \tiny
  \setlength{\tabcolsep}{1pt}
  \renewcommand{\arraystretch}{0.96}
  \begin{tabular}{lcccccc}
    \toprule
    Horizon & Pers. & Glob. lin. & Glob. MLP & Glob. RF & Glob. UA & Ent. lin. \\
    \midrule
    1 minute & 3.716 & 3.629 & 3.735 & 4.496 & 3.934 & 3.633 \\
    5 minutes & 6.268 & 5.503 & 5.722 & 6.293 & 6.328 & 5.520 \\
    10 minutes & 6.932 & 5.953 & 6.286 & 6.642 & 6.648 & 5.966 \\
    \bottomrule
  \end{tabular}
\end{table}

Pooled linear regression transfers almost as well as entity-specific linear regression: its mean MAE differs by only $-0.004$, $-0.017$, and $-0.013$ at the 1-, 5-, and 10-minute horizons. The global MLP is the next-best zero-shot point predictor, but still trails global linear regression at every horizon.

Global \texttt{UA-MSTCN-Lite} does not beat the linear point baseline, but its held-out-machine $P90$ coverage remains 0.904, 0.859, and 0.853. The transfer result therefore preserves the same boundary: linear point forecasts lead, while the surrogate supplies controller-facing uncertainty.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.78\linewidth]{../experiments/results/machine_transfer/figures/machine_transfer_zero_shot_mae.png}
  \caption{Cross-machine zero-shot transfer performance across the 3 deterministic folds.}
  \label{fig:machine-transfer}
\end{figure}

\subsection{Container service-level evidence}

The service cohort narrows the construct-validity gap by moving from aggregate and machine abstractions to 139 audited \texttt{app\_du} groups over the shared 11{,}520-minute span. The overlap audit retains all 10 earlier targeted services, but the broader selection changes the service-level headline: point-forecast leadership shifts to linear regression, and the earlier action-reduction claim no longer survives.

Table~\ref{tab:service-forecasting-summary} shows that linear regression is strongest at every horizon (MAE 19.999, 34.250, and 43.463). Persistence is the overall runner-up, and the MLP remains ahead of \texttt{UA-MSTCN-Lite} but behind both linear regression and persistence. The earlier targeted ranking favoring \texttt{UA-MSTCN-Lite} does not survive broader audited service coverage.

\begin{table}[htbp]
  \centering
  \caption{Service-level forecasting across the audited 139-service \texttt{app\_du} cohort.}
  \label{tab:service-forecasting-summary}
  \scriptsize
  \setlength{\tabcolsep}{2pt}
  \renewcommand{\arraystretch}{0.96}
  \begin{tabular}{lcccc}
    \toprule
    Model & 1 min. & 5 min. & 10 min. & Avg. \\
    \midrule
    Persistence & 20.408 & 34.638 & 45.098 & 33.381 \\
    Linear regression & 19.999 & 34.250 & 43.463 & 32.571 \\
    MLP regressor & 23.684 & 38.395 & 49.117 & 37.066 \\
    Random forest & 26.317 & 37.728 & 44.912 & 36.319 \\
    UA-MSTCN-Lite & 27.939 & 39.746 & 45.677 & 37.787 \\
    \bottomrule
  \end{tabular}
\end{table}

Figure~\ref{fig:service-forecasting} shows the same result visually. The broader service route matters because it exposes heterogeneity that a small targeted subset missed.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.82\linewidth]{../experiments/results/service_cohort_broad/figures/service_forecasting_mae.png}
  \caption{Average MAE across the audited 139-service \texttt{app\_du} cohort. Under broader service coverage, point-forecast leadership shifts to linear regression and the generic MLP baseline remains mid-pack rather than dominant.}
  \label{fig:service-forecasting}
\end{figure}

At service level, \texttt{UA-MSTCN-Lite} still provides usable upper quantiles: $P90$ coverage is 0.921, 0.896, and 0.877 at the three horizons. $P50$ coverage is slightly low (0.469--0.481), but the upper quantile remains close enough for the predictive-controller test.

The service-level control comparison is stricter because demand is heterogeneous. Averaged over 139 services, lagged target tracking lowers over-provisioning to 3.848 but raises SLA violation to 0.486 and actions to 1660.9. Relative to reactive control, the predictive policy raises mean SLA violation from 0.0173 to 0.0311 and actions from 249.0 to 399.4, while mean over-provisioning falls from 56.310 to 53.677.

The paired service bootstrap fixes the headline. Predictive-minus-reactive SLA delta is +0.0138 with 95\% interval [0.0035, 0.0271], and scaling-actions delta is +150.4 with interval [114.0, 185.0], so both degradations are stable. Over-provisioning differs: the mean delta is -2.633, but its interval [-6.953, 2.328] crosses zero. The auxiliary load-weighted action delta is nearly zero (-0.567 with interval [-64.257, 68.245]), indicating that the equal-weighted action penalty comes mainly from many smaller services rather than the highest-load services alone.

Figure~\ref{fig:service-policy-delta-distribution} shows the same heterogeneity: SLA and action deltas stay positive through most service ranks, while over-provisioning straddles zero for many services.

\begin{table}[htbp]
  \centering
  \caption{Lightweight mechanism summary for predictive-minus-reactive service outcomes on the audited broad cohort. Panel A reports equal-weighted mean deltas after rank-based median splits on selected service descriptors. Panel B reports an outcome taxonomy; near-neutral requires all three absolute deltas to fall below the cohort-specific first-quartile absolute-delta thresholds. Lower is better for all three metrics.}
  \label{tab:service-policy-mechanism}
  \fontsize{7.3}{8.1}\selectfont
  \setlength{\tabcolsep}{2.2pt}
  \renewcommand{\arraystretch}{0.93}
  \begin{tabular}{@{}llrrrr@{}}
    \toprule
    \multicolumn{6}{l}{\textbf{Panel A: Descriptor splits}} \\
    Split & Half & Svcs & SLA $\Delta$ & Over $\Delta$ & Acts $\Delta$ \\
    \midrule
    Mean load & High & 69 & +0.011 & -0.800 & +45.4 \\
    Mean load & Low & 70 & +0.017 & -4.440 & +254.0 \\
    Burstiness & High & 69 & +0.023 & +4.970 & +82.2 \\
    Burstiness & Low & 70 & +0.005 & -10.127 & +217.7 \\
    Lag-1 autocorr. & High & 69 & +0.026 & -5.731 & +95.0 \\
    Lag-1 autocorr. & Low & 70 & +0.002 & +0.421 & +205.1 \\
    Imputation ratio & High & 69 & +0.018 & +0.105 & +144.7 \\
    Imputation ratio & Low & 70 & +0.010 & -5.332 & +156.1 \\
    \bottomrule
  \end{tabular}

  \vspace{0.45em}

  \begin{tabular}{@{}p{0.42\columnwidth}rrr@{}}
    \toprule
    \multicolumn{4}{l}{\textbf{Panel B: Outcome taxonomy}} \\
    Category & Svcs & Share (\%) & Load share (\%) \\
    \midrule
    SLA improved, Over improved & 6 & 4.3 & 6.5 \\
    SLA improved, Actions worsened & 7 & 5.0 & 4.7 \\
    Over improved, SLA worsened & 87 & 62.6 & 47.9 \\
    All worsened & 12 & 8.6 & 4.9 \\
    Near-neutral & 3 & 2.2 & 3.0 \\
    Other mixed & 24 & 17.3 & 33.0 \\
    \bottomrule
  \end{tabular}
\end{table}

Table~\ref{tab:service-policy-mechanism} adds a mechanism view. SLA degradation is strongest in high-burstiness (+0.0227) and high-autocorrelation (+0.0255) services, while over-provisioning gains concentrate in low-burstiness (-10.127) and low-load (-4.440) halves. The dominant category is ``over improved, SLA worsened'' (87 of 139 services); only 6 services improve both SLA and over-provisioning. Thus the shared global predictive policy mainly trades lower over-provisioning for worse SLA and actions, with the harshest SLA penalties in burstier and more persistent services.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../experiments/results/service_cohort_broad/figures/service_policy_delta_distribution.png}
  \caption{Sorted predictive-minus-reactive deltas across the audited 139-service cohort. Negative values are favorable for all three metrics. The positive centers of the SLA and scaling-actions panels show that those penalties are widespread rather than caused only by a few extreme services, while over-provisioning remains heterogeneous around zero.}
  \label{fig:service-policy-delta-distribution}
\end{figure}

\subsection{Trace-driven control comparison}

Table~\ref{tab:policy-results} summarizes aggregate control. Reactive control achieves near-zero SLA violation by carrying mean capacity 0.821 against average required capacity 0.470. Lagged target tracking nearly eliminates over-provisioning but incurs 47.1\% SLA violation and 1616 actions. The predictive policy sits between them: SLA violation rises to 3.46\%, but over-provisioning falls by 59.8\% relative to reactive control, with mean capacity 0.610. All policies share the same normalized required-capacity space.

\begin{table}[htbp]
  \centering
  \caption{Reactive, lagged-target, and predictive capacity control on the 5-minute evaluation horizon.}
  \label{tab:policy-results}
  \scriptsize
  \setlength{\tabcolsep}{1.5pt}
  \renewcommand{\arraystretch}{0.96}
  \begin{tabular}{lccccc}
    \toprule
    Policy & SLA & Over & Under & Acts & Cap. \\
    \midrule
    Reactive threshold & 0.001 & 0.351 & 0.000 & 43 & 0.821 \\
    Lagged target tracking & 0.471 & 0.013 & 0.014 & 1616 & 0.469 \\
    Predictive & 0.035 & 0.141 & 0.001 & 589 & 0.610 \\
    \bottomrule
  \end{tabular}
\end{table}

Using QLR as the predictive uncertainty source gives the same aggregate boundary: SLA violation 0.034, over-provisioning 0.147, and 587 actions at the 5-minute horizon. QLR is safer than lagged target tracking but worse than reactive control on SLA and actions, and it is not treated as a full 139-service result.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../experiments/results/figures/policy_capacity_comparison.png}
  \caption{Reactive, lagged-target, and predictive capacity trajectories on the 5-minute evaluation horizon. The lagged baseline shows the failure mode of naive target chasing, while the predictive policy follows the calibrated high-quantile forecast with far lower SLA risk.}
  \label{fig:policy-capacity}
\end{figure}

\subsection{Policy ablation and sensitivity}

Table~\ref{tab:policy-ablation} shows that the controller depends mainly on the scale-out margin and cooldown. Removing the margin raises SLA violation from 0.0346 to 0.1137; removing cooldown raises actions from 589 to 1617 and SLA violation to 0.1119. A median-only policy is unsafe (0.3337 SLA violation), while removing the scale-in guard has almost no effect.

\begin{table}[htbp]
  \centering
  \caption{Predictive-policy ablation on the 5-minute evaluation route.}
  \label{tab:policy-ablation}
  \scriptsize
  \setlength{\tabcolsep}{1.5pt}
  \begin{tabular}{p{1.65cm}ccccc}
    \toprule
    Variant & SLA & Over & Under & Acts & Cap. \\
    \midrule
    Full predictive policy & 0.035 & 0.141 & 0.001 & 589 & 0.610 \\
    No scale-out margin & 0.114 & 0.089 & 0.004 & 589 & 0.555 \\
    No cooldown & 0.112 & 0.100 & 0.005 & 1617 & 0.565 \\
    Median only & 0.334 & 0.035 & 0.017 & 359 & 0.488 \\
    No scale-in guard & 0.035 & 0.141 & 0.001 & 589 & 0.610 \\
    \bottomrule
  \end{tabular}
\end{table}

Figure~\ref{fig:policy-pareto} sweeps scale-out margin and cooldown. The default $(m=0.10, c=8)$ is not optimal on a single axis, but it sits near the middle of the observed Pareto surface: smaller margins reduce over-provisioning but raise SLA risk, while larger margins and longer cooldown consume more steady capacity.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../experiments/results/figures/policy_pareto_sensitivity.png}
  \caption{Sensitivity of the predictive controller to scale-out margin and cooldown. The selected default operates in the middle of the observed SLA-versus-over-provisioning trade-off.}
  \label{fig:policy-pareto}
\end{figure}

The final boundary is narrow: the simulator provides reproducible trace-level evidence about when predictive control helps or fails, but it does not prove that the same policy transfers unchanged to a live cluster controller.
